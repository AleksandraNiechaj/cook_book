{% extends 'base.html.twig' %}

{% block title %}Edycja profilu{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Edycja profilu</h1>

        {{ form_start(form) }}
        {{ form_row(form.email) }}
        <button class="btn btn-primary mt-3">💾 Zapisz zmiany</button>
        {{ form_end(form) }}

        <p class="mt-3">
            <a href="{{ path('app_admin') }}" class="btn btn-secondary">← Powrót</a>
        </p>
    </div>
{% endblock %}
