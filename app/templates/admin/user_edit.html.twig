{% extends 'base.html.twig' %}
{% block title %}Edytuj użytkownika{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Edytuj użytkownika #{{ item.id }}</h1>
        <p class="text-muted mb-3">{{ item.email }}</p>

        {{ form_start(form) }}
        {{ form_row(form.email) }}
        {{ form_row(form.roles) }}
        <button class="btn btn-primary mt-2">💾 Zapisz</button>
        {{ form_end(form) }}

        <p class="mt-3">
            <a href="{{ path('admin_user_index') }}" class="btn btn-outline-secondary btn-sm">← Lista użytkowników</a>
        </p>
    </div>
{% endblock %}
