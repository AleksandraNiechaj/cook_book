<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}{{ 'app.title'|trans }}{% endblock %}</title>

    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    {% block stylesheets %}{% endblock %}
    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
    <style>
        /* Prosty, responsywny layout bez zewnętrznych bibliotek */
        body { margin: 0; font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, Arial, "Apple Color Emoji", "Segoe UI Emoji"; }
        .container { max-width: 1000px; margin: 0 auto; padding: 1rem; }
        .navbar { display: flex; gap: .75rem; align-items: center; justify-content: space-between; padding: .75rem 1rem; border-bottom: 1px solid #e5e7eb; }
        .nav-left, .nav-right { display: flex; gap: .75rem; align-items: center; flex-wrap: wrap; }
        .nav-link { text-decoration: none; color: #111827; padding: .25rem .5rem; border-radius: .375rem; }
        .nav-link:hover { background: #f3f4f6; }
        .brand { font-weight: 700; }
        .flashes { margin: 1rem 0; }
        .flash { padding: .75rem 1rem; border-radius: .5rem; margin-bottom: .5rem; border: 1px solid #e5e7eb; }
        .flash-success { background: #ecfdf5; border-color: #10b98133; }
        .flash-error, .flash-danger { background: #fef2f2; border-color: #ef444433; }
        .flash-warning { background: #fffbeb; border-color: #f59e0b33; }
        main { padding: 1rem; }
    </style>
</head>
<body>
<header class="navbar">
    <div class="nav-left">
        <a class="nav-link brand" href="{{ path('app_home') }}">{{ 'app.title'|trans }}</a>
    </div>
    <nav class="nav-right">
        {% if app.user %}
            <span>{{ app.user.email }}</span>

            <a class="nav-link" href="{{ path('account_profile_edit') }}">{{ 'nav.my_account'|trans }}</a>

            {# 👇 Link „Users” tylko dla admina #}
            {% if is_granted('ROLE_ADMIN') %}
                <a class="nav-link" href="{{ path('admin_user_index') }}">Users</a>
            {% endif %}

            <a class="nav-link" href="{{ path('account_password_change') }}">{{ 'nav.change_password'|trans }}</a>
            <a class="nav-link" href="{{ path('app_logout') }}">{{ 'nav.logout'|trans }}</a>
        {% else %}
            <a class="nav-link" href="{{ path('app_login') }}">{{ 'nav.login'|trans }}</a>
            <a class="nav-link" href="{{ path('auth_register') }}">{{ 'nav.register'|trans }}</a>
        {% endif %}
    </nav>
</header>

<div class="container">
    <div class="flashes">
        {# Wyświetlanie wszystkich komunikatów flash (sukces/błąd/ostrzeżenie) #}
        {% for type, messages in app.flashes %}
            {% for message in messages %}
                <div class="flash flash-{{ type }}">{{ message|trans }}</div>
            {% endfor %}
        {% endfor %}
    </div>

    <main>
        {% block body %}{% endblock %}
    </main>
</div>
</body>
</html>
