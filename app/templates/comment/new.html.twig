{% extends 'base.html.twig' %}

{% block title %}Dodaj komentarz{% endblock %}

{% block body %}
    <h2>Dodaj komentarz do przepisu: {{ recipe.title }}</h2>

    {{ form_start(form, {'action': path('app_comment_add', { id: recipe.id })}) }}
    {{ form_row(form.content) }}
    {{ form_row(form.rating) }}
    <button class="btn btn-success mt-2">💬 Dodaj komentarz</button>
    {{ form_end(form) }}

    <p class="mt-3">
        <a href="{{ path('recipe_show', { id: recipe.id }) }}" class="btn btn-secondary">← Powrót do przepisu</a>
    </p>
{% endblock %}
